use ferrite_session::prelude::*;
fn sender() -> Session<ReceiveChannel<A, ReceiveChannel<A,
        ReceiveChannel<ReceiveChannel<A, ReceiveChannel<A, End>>,
            End
        >
    >
> >{ @SYNTHESIZE [] }
