use std::format;

use ferrite_session::prelude::*;

fn myFunc() -> Session<Rec<Either<ReceiveValue<Int, End>, Z>>> {
    fix_session(offer_choice! { Left: receive_value(|val_0| {terminate ()}), Right: myFunc() })
}
