use std::format;

use ferrite_session::{
  either::*,
  prelude::*,
};

type myType = ReceiveChannel<ExternalChoice<Either<ReceiveChannel<A,B>, ReceiveChannel<A, C>>>, ReceiveChannel<A, ExternalChoice<Either<SendChannel<B, End>, SendChannel<C, End>>>>>;

fn myFunc() -> Session<myType> { @SYNTHESIZE [] }

fn user(arg1: A: 1, arg2: ExternalChoice<Either<ReceiveChannel<A,B>, ReceiveChannel<A, C>>>: 1) -> Session<ReceiveChannel<myType, SendChannel<C, End>>> { @SYNTHESIZE [] }

fn main(arg1: A: 1, arg2: ExternalChoice<Either<ReceiveChannel<A,B>, ReceiveChannel<A, C>>>: 1) -> Session<SendChannel<C, End>> { @SYNTHESIZE [use myFunc, user;] }
