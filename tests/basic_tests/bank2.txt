use ferrite_session::{prelude::*};

define_choice! {
    BankOps;
    deposit: ReceiveValue<Int, End>,
    withdraw: SendValue<Int, ReceiveValue<Int, End>>,
    Terminate: End
}

fn bankops(arg1: Int) -> Session<ExternalChoice<BankOps>> { @SYNTHESIZE []}

fn bank_session(withdraw_value: Int) -> Session<ReceiveValue<String, ExternalChoice<BankOps>>> { @SYNTHESIZE [use bankops;] }

fn client_session(myId: String, depositAmt: Int) -> Session< ReceiveChannel< ReceiveValue<String, ExternalChoice<BankOps>>, End>> { @SYNTHESIZE [] }
