use ferrite_session::{prelude::*};

define_choice! {
    BankOps;
    deposit: ReceiveValue<Int, End>,
    withdraw: SendValue<Int, ReceiveValue<Int, End>>,
    Terminate: End
}

fn bankops(arg1: Int: 1) -> Session<ExternalChoice<BankOps>> { @SYNTHESIZE []}

fn bank_session(withdraw_value: Int: 1) -> Session<ReceiveValue<String, ExternalChoice<BankOps>>> { @SYNTHESIZE [] }

fn client_session(myId: String: 1, depositAmt: Int: 1) -> Session< ReceiveChannel< ReceiveValue<String, ExternalChoice<BankOps>>, End>> { @SYNTHESIZE [] }
