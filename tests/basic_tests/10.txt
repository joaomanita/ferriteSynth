use std::format;

use ferrite_session::prelude::*;

type myType = Rec<
    ExternalChoice<
      l: SendValue<Int, Z>,
      r: End
    >
  >;

fn myFunc() -> Session<ReceiveChannel<myType, SendValue<Int, SendValue<Int, End>>>> { @SYNTHESIZE }
