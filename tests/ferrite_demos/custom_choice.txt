use ferrite_session::prelude::*;

define_choice! {
    FooBarBaz;
    Foo: SendValue<String, End>,
    Bar: ReceiveValue<u64, End>,
    Baz: End
}

fn provider(arg1: String: 1) -> Session<ExternalChoice<FooBarBaz>> { @SYNTHESIZE  []}

fn client() -> Session<ReceiveChannel<ExternalChoice<FooBarBaz>, End>> { @SYNTHESIZE  []}

fn main(arg1:String: 1) { @SYNTHESIZE [use provider, client;] }

