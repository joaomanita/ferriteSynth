use ferrite_session::{
  either::*,
  prelude::*,
};

define_choice! {
    StreamChoice;
    rec1: Rec<InternalChoice<Either<SendValue<String, Z>, S<Z>>>>,
    end1: ReceiveValue<String, End>
}

type Stream = Rec<
  ExternalChoice<
    StreamChoice
  >
>;

fn producer(arg1: String) -> Session<Stream> { @SYNTHESIZE  []}
