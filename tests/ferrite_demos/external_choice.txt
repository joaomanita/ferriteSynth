use ferrite_session::{
  either::*,
  prelude::*,
};

fn creator() -> Session<SendChannel<A, End>> {}

fn provider() -> Session<ExternalChoice<Either<SendChannel<A, End>, ReceiveValue<u64, End>>>> { @SYNTHESIZE [use creator;]}

fn client() -> Session<ReceiveChannel<ExternalChoice<Either<SendValue<String, End>, ReceiveValue<u64, End>>>, End>> { @SYNTHESIZE []}
