use ferrite_session::{
  either::*,
  prelude::*,
};

fn creator() -> Session<SendValue<String, End>> {}

fn provider<A>() -> Session<ExternalChoice<Either<SendValue<String, End>, ReceiveValue<u64, End>>>> { @SYNTHESIZE [suggest creator;]}

fn client() -> Session<ReceiveChannel<ExternalChoice<Either<SendValue<String, End>, ReceiveValue<u64, End>>>, End>> { @SYNTHESIZE [] }

fn main() { @SYNTHESIZE [use provider, client;]}
