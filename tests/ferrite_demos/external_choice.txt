use ferrite_session::{
  either::*,
  prelude::*,
};

fn provider(arg1: String) -> Session<ExternalChoice<Either<SendValue<String, End>, ReceiveValue<u64, End>>>> { @SYNTHESIZE []}

fn client() -> Session<ReceiveChannel<ExternalChoice<Either<SendValue<String, End>, ReceiveValue<u64, End>>>, End>> { @SYNTHESIZE []}
