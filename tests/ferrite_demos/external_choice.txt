use ferrite_session::{
  either::*,
  prelude::*,
};

fn provider(arg1: String) -> ExternalChoice<l: SendValue<String, End>, r: ReceiveValue<u64, End>> { @SYNTHESIZE }

fn client() -> ReceiveChannel<ExternalChoice<l: SendValue<String, End>, r: ReceiveValue<u64, End>>, End> { @SYNTHESIZE }
